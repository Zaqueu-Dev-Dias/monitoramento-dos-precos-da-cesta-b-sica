import 'package:flutter/material.dart';

class Nome_produto extends StatelessWidget {
  const Nome_produto({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.white,child: Center(child: TextField()),);
  }
}
